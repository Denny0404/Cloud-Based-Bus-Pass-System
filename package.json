{
  "scripts": {
    "cypress:open": "npx cypress open",
    "cypress:run": "cypress run",
    "test": "npx cypress run --browser chrome --spec \"cypress/e2e/test/busPass.cy.js\" --reporter mochawesome",
    "cypress:report": "npx mochawesome-merge cypress/reports/*.json -o cypress/reports/merged-report.json && npx marge cypress/reports/merged-report.json -o cypress/reports"
  },
  "devDependencies": {
    "cypress": "^14.2.1",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
  
}
